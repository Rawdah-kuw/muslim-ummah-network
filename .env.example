# Guardrailed search (Google Programmable Search Engine)
# 1. Create an engine at https://programmablesearchengine.google.com
#    → "Search only included sites" → add the approved domains
# 2. Get an API key at https://console.cloud.google.com (enable "Custom Search API")
# Without these, the search gracefully falls back to a scoped external Google search.
GOOGLE_KEY=
ENGINE_ID=
